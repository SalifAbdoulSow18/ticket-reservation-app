apiVersion: v1
kind: Service
metadata:
  name: ticket-reservation-service
  namespace: ticket-reservation
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: ticket-reservation